{
  "name": "tiktok-clone-v2-frontend",
  "version": "2.2.0",
  "private": true,
  "description": "TikTok Clone V2 frontend with a same-origin API proxy and reliable byte-range video cache.",
  "type": "commonjs",
  "engines": {
    "node": ">=18 <25"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/sw.js",
    "build": "npm run check",
    "test": "npm run check"
  }
}
